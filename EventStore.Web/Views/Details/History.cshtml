@model EventStore.Web.Models.ViewHistoryModel

@{
    ViewBag.Title = "View Entity History";
}

<br />

<table cellpadding="15px">

    <tr>
        <td></td>
        <td>Historical Value</td>
        <td>Current Value</td>
    </tr>

    @foreach (var histItem in Model.HospitalHistory)
    {
        <tr
            @if (histItem.Id != Model.Current.Id)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Id: </strong></td>
            <td>@histItem.Id</td>
            <td>@Model.Current.Id</td>
        </tr>

        <tr
            @if (histItem.StreamId.ToString() != Model.Current.StreamId.ToString())
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Stream Id: </strong></td>
            <td>@histItem.StreamId.ToString()</td>
            <td>@Model.Current.StreamId.ToString()</td>
        </tr>

        <tr
            @if (histItem.HospitalId != Model.Current.HospitalId)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Hospital Id: </strong></td>
            <td>@histItem.HospitalId</td>
            <td>@Model.Current.HospitalId</td>
        </tr>

        <tr
            @if (histItem.Name != Model.Current.Name)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Name: </strong></td>
            <td>@histItem.Name</td>
            <td>@Model.Current.Name</td>
        </tr>

        <tr
            @if (histItem.Address != Model.Current.Address)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Address: </strong></td>
            <td>@histItem.Address</td>
            <td>@Model.Current.Address</td>
        </tr>

        <tr
            @if (histItem.City != Model.Current.City)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>City: </strong></td>
            <td>@histItem.City</td>
            <td>@Model.Current.City</td>
        </tr>

        <tr
            @if (histItem.State != Model.Current.State)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>State: </strong></td>
            <td>@histItem.State</td>
            <td>@Model.Current.State</td>
        </tr>

        <tr
            @if (histItem.ZipCode != Model.Current.ZipCode)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>ZipCode: </strong></td>
            <td>@histItem.ZipCode</td>
            <td>@Model.Current.ZipCode</td>
        </tr>

        <tr
            @if (histItem.CountyName != Model.Current.CountyName)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>County: </strong></td>
            <td>@histItem.CountyName</td>
            <td>@Model.Current.CountyName</td>
        </tr>

        <tr
            @if (histItem.PhoneNumber != Model.Current.PhoneNumber)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Phone Number: </strong></td>
            <td>@histItem.PhoneNumber</td>
            <td>@Model.Current.PhoneNumber</td>
        </tr>

        <tr
            @if (histItem.Type != Model.Current.Type)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Type: </strong></td>
            <td>@histItem.Type</td>
            <td>@Model.Current.Type</td>
        </tr>

        <tr
            @if (histItem.Ownership != Model.Current.Ownership)
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Ownership: </strong></td>
            <td>@histItem.Ownership</td>
            <td>@Model.Current.Ownership</td>
        </tr>

        <tr
            @if (histItem.EmergencyServices.ToString() != Model.Current.EmergencyServices.ToString())
            {
                @:bgcolor="red"
            }
            >
            <td><strong>Emergency Services: </strong></td>
            <td>@histItem.EmergencyServices.ToString()</td>
            <td>@Model.Current.EmergencyServices.ToString()</td>
        </tr>
        <tr>
            <td colspan="3">
                <hr />
            </td>
        </tr>
    }


    <tr>
        <td colspan="3">
            @Html.ActionLink("<< Back to entity detail", "ViewEntityDetails", new { CollectionName = "", EntityId = Model.Current.Id })
        </td>
    </tr>
</table>